{
  "name": "rowel-launch-video",
  "private": true,
  "version": "0.0.0",
  "description": "The launch video, as code. Deliberately not a workspace of the repository root: it needs React and a renderer, and the projects that ship have no dependencies at all.",
  "scripts": {
    "studio": "remotion studio",
    "render": "node render.mjs"
  },
  "dependencies": {
    "@remotion/cli": "4.0.520",
    "react": "19.0.0",
    "react-dom": "19.0.0",
    "remotion": "4.0.520"
  }
}
